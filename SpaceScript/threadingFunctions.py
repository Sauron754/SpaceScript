import SpaceScript
import multiprocessing
from SpaceScript import frontEnd
from SpaceScript.frontEnd import terminal as terminal

def simThread(queues_arr, pipes_arr, holdValue, objectArray_arr):


def termThread(queues_arr, pipes_arr, holdValue, objectArray_arr):
	initialized_bool = True

	while initialized_bool:
		
def lowTermThread(queues_arr)